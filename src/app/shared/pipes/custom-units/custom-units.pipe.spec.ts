import { CustomUnitsPipe } from './custom-units.pipe';

describe('CustomUnitsPipe', () => {
  it('create an instance', () => {
    const pipe = new CustomUnitsPipe();
    expect(pipe).toBeTruthy();
  });
});
